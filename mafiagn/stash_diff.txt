diff --git a/mafiagn/cart.js b/mafiagn/cart.js
index 4bbbbd2..d54c4b1 100644
--- a/mafiagn/cart.js
+++ b/mafiagn/cart.js
@@ -45,6 +45,8 @@
     showMiniToast(`${product.name} adicionado ao carrinho`);
   }
 
+  window.gnAddToCart = addToCart;
+
   function showMiniToast(text) {
     const t = document.createElement('div');
     t.textContent = text;
@@ -544,6 +546,10 @@
 
       window.open(`https://wa.me/${STORE_PHONE}?text=${encodeURIComponent(message)}`, '_blank');
 
+      // Limpa o carrinho
+      saveCart({ items: [] });
+      updateHeader({ items: [] });
+
       // Registra pedido no banco
       const emailCliente = localStorage.getItem('gn_profile_email') || '';
       fetch(`${SUPABASE_URL}/rest/v1/pedidos`, {
diff --git a/mafiagn/styles.css b/mafiagn/styles.css
index f15b0a8..b702241 100644
--- a/mafiagn/styles.css
+++ b/mafiagn/styles.css
@@ -140,6 +140,10 @@ body {
   cursor: pointer;
 }
 
+.brand-name-mobile {
+  display: none;
+}
+
 /* ── HERO ── */
 .hero {
   position: relative;
@@ -699,14 +703,18 @@ body {
 /* ── FOOTER ── */
 .footer {
   display: grid;
-  grid-template-columns: 200px repeat(4, 1fr);
-  gap: 40px;
+  grid-template-columns: repeat(7, 1fr);
+  gap: 32px;
   max-width: 1280px;
   margin: 0 auto;
   padding: 48px 40px 32px;
   border-top: 1px solid var(--border);
 }
 
+.footer-brand {
+  grid-column: 1 / 2;
+}
+
 .footer-logo {
   height: 80px;
   width: auto;
@@ -727,13 +735,17 @@ body {
 }
 
 .footer-social a {
-  font-size: 1.2rem;
+  display: inline-block;
+  font-size: 0.82rem;
+  color: var(--muted);
   text-decoration: none;
-  opacity: 0.7;
-  transition: opacity 0.2s;
+  margin-bottom: 4px;
+  transition: color 0.2s;
 }
 
-.footer-social a:hover { opacity: 1; }
+.footer-social a:hover {
+  color: var(--gold-light);
+}
 
 .footer-col h4 {
   font-size: 0.78rem;
@@ -1349,8 +1361,7 @@ body {
 
 /* ── RESPONSIVE ── */
 @media (max-width: 1100px) {
-  .footer { grid-template-columns: repeat(3, 1fr); }
-  .footer-brand { grid-column: 1 / -1; }
+  .footer { grid-template-columns: repeat(4, 1fr); }
   .product-grid { grid-template-columns: repeat(2, 1fr); }
 }
 
@@ -1392,8 +1403,7 @@ body {
 
 /* Tablet / iPad */
 @media (max-width: 1100px) {
-  .footer { grid-template-columns: repeat(3, 1fr); }
-  .footer-brand { grid-column: 1 / -1; }
+  .footer { grid-template-columns: repeat(4, 1fr); }
   .product-grid { grid-template-columns: repeat(2, 1fr); }
   .main-header { padding: 12px 24px; }
   .hero-logo { height: 150px; }
@@ -1518,147 +1528,1418 @@ body {
   .footer { gap: 20px; }
 }
 
-/* ── SORTEIOS ── */
-.sorteios-desc {
+/* -- MONTE SEU KIT 3D -- */
+.kit-desc {
   color: var(--muted);
   font-size: 0.9rem;
-  margin-bottom: 28px;
+  margin-bottom: 32px;
 }
+.kit-desc strong { color: var(--gold-light); }
 
-.sorteio-loading,
-.sorteio-empty {
+.kit-3d-layout {
+  display: grid;
+  grid-template-columns: 1fr 340px 1fr;
+  gap: 24px;
+  align-items: start;
+  margin-bottom: 36px;
+}
+
+.kit-3d-col {
+  display: flex;
+  flex-direction: column;
+}
+
+.kit-3d-center {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 10px;
+  position: sticky;
+  top: 90px;
+}
+
+.kit-3d-hint {
+  font-size: 0.72rem;
   color: var(--muted);
-  font-size: 0.9rem;
-  padding: 40px;
-  text-align: center;
-  grid-column: 1 / -1;
+  letter-spacing: 0.06em;
 }
 
-.sorteios-grid {
-  display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
-  gap: 20px;
+.kit-option-imgs {
+  display: flex;
+  gap: 4px;
+  flex-shrink: 0;
 }
 
-.sorteio-card {
-  background: linear-gradient(135deg, #141010, #0f0d0d);
-  border: 1px solid rgba(213,166,81,0.3);
-  border-radius: 16px;
-  padding: 28px 24px;
+.kit-option-imgs img {
+  width: 48px;
+  height: 48px;
+  object-fit: cover;
+  border-radius: 5px;
+}
+
+.kit-preview-slot.has-back {
   display: grid;
-  gap: 12px;
-  position: relative;
-  overflow: hidden;
-  transition: transform 0.2s, border-color 0.2s;
+  grid-template-columns: 1fr 1fr;
+  gap: 0;
 }
 
-.sorteio-card:hover { transform: translateY(-2px); border-color: rgba(213,166,81,0.6); }
+.kit-preview-slot.has-back img {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  display: block;
+}
 
-.sorteio-card::before {
-  content: '';
-  position: absolute;
-  top: -40px; right: -40px;
-  width: 120px; height: 120px;
-  border-radius: 50%;
-  background: radial-gradient(circle, rgba(213,166,81,0.12), transparent 70%);
-  pointer-events: none;
+.kit-preview-slot.has-back img:first-child {
+  border-radius: var(--radius) 0 0 var(--radius);
 }
 
-.sorteio-esgotado {
-  border-color: rgba(255,255,255,0.08);
-  opacity: 0.6;
+.kit-preview-slot.has-back img:last-child {
+  border-radius: 0 var(--radius) var(--radius) 0;
 }
 
-.sorteio-esgotado:hover { transform: none; border-color: rgba(255,255,255,0.08); }
+.kit-preview {
+  display: flex;
+  flex-direction: column;
+  gap: 12px;
+  width: 300px;
+}
 
-.sorteio-badge {
-  display: inline-block;
-  padding: 4px 12px;
-  border-radius: 20px;
-  font-size: 0.68rem;
-  font-weight: 800;
-  letter-spacing: 0.12em;
-  width: fit-content;
+.kit-preview-slot {
+  border: 2px dashed rgba(213,166,81,0.25);
+  border-radius: var(--radius);
+  overflow: hidden;
+  background: #111010;
+  aspect-ratio: 4/3;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  transition: border-color 0.2s;
 }
 
-.sorteio-card:not(.sorteio-esgotado) .sorteio-badge {
-  background: rgba(76,175,80,0.15);
-  border: 1px solid rgba(76,175,80,0.4);
-  color: #4caf50;
+.kit-preview-slot.filled {
+  border-style: solid;
+  border-color: rgba(213,166,81,0.5);
 }
 
-.sorteio-esgotado .sorteio-badge {
-  background: rgba(255,255,255,0.05);
-  border: 1px solid rgba(255,255,255,0.1);
+.kit-preview-slot img {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  display: block;
+}
+
+.kit-preview-placeholder {
   color: var(--muted);
+  font-size: 0.82rem;
+  text-align: center;
+  line-height: 2;
+  letter-spacing: 0.04em;
 }
 
-.sorteio-desconto {
-  font-family: 'Cinzel', serif;
-  font-size: 2.4rem;
-  font-weight: 900;
+.kit-3d-labels {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 4px;
+  width: 100%;
+  padding: 10px 14px;
+  background: rgba(213,166,81,0.06);
+  border: 1px solid rgba(213,166,81,0.18);
+  border-radius: 8px;
+}
+
+.kit-3d-labels span {
+  font-size: 0.75rem;
   color: var(--gold-light);
-  line-height: 1;
+  letter-spacing: 0.06em;
+  font-weight: 600;
 }
 
-.sorteio-esgotado .sorteio-desconto { color: var(--muted); }
+.kit-col-title {
+  font-size: 0.82rem;
+  letter-spacing: 0.14em;
+  color: var(--gold-light);
+  margin-bottom: 16px;
+}
 
-.sorteio-codigo {
-  font-size: 1.2rem;
-  font-weight: 800;
-  letter-spacing: 0.18em;
-  color: var(--text);
-  background: rgba(213,166,81,0.08);
-  border: 1px dashed rgba(213,166,81,0.35);
-  border-radius: 8px;
-  padding: 10px 14px;
-  text-align: center;
+.kit-options {
+  display: grid;
+  grid-template-columns: 1fr;
+  gap: 10px;
 }
 
-.codigo-riscado {
-  text-decoration: line-through;
-  color: var(--muted);
-  background: rgba(255,255,255,0.03);
-  border-color: rgba(255,255,255,0.08);
+.kit-option {
+  display: flex;
+  align-items: center;
+  gap: 12px;
+  border: 2px solid var(--border);
+  border-radius: var(--radius);
+  overflow: hidden;
+  cursor: pointer;
+  transition: border-color 0.2s, transform 0.2s;
+  background: var(--surface);
+  padding: 6px;
 }
 
-.sorteio-vagas {
-  font-size: 0.8rem;
+.kit-option:hover { border-color: rgba(213,166,81,0.4); transform: translateX(3px); }
+.kit-option.kit-selected { border-color: var(--gold-light); box-shadow: 0 0 0 1px var(--gold-light); }
+
+.kit-option img {
+  width: 64px;
+  height: 64px;
+  object-fit: cover;
+  border-radius: 6px;
+  flex-shrink: 0;
+}
+
+.kit-option span {
+  font-size: 0.78rem;
   color: var(--muted);
+  font-weight: 500;
+  flex: 1;
 }
 
-.sorteio-barra-wrap {
-  height: 5px;
-  background: rgba(255,255,255,0.06);
-  border-radius: 999px;
-  overflow: hidden;
+.kit-option.kit-selected span { color: var(--gold-light); }
+
+.kit-summary {
+  background: rgba(213,166,81,0.05);
+  border: 1px solid rgba(213,166,81,0.2);
+  border-radius: 16px;
+  padding: 24px 28px;
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 20px;
+  flex-wrap: wrap;
 }
 
-.sorteio-barra {
-  height: 100%;
-  background: linear-gradient(90deg, var(--gold), var(--gold-light));
-  border-radius: 999px;
-  transition: width 0.4s ease;
+.kit-total {
+  display: flex;
+  align-items: center;
+  gap: 12px;
+  font-size: 1rem;
 }
 
-.sorteio-esgotado .sorteio-barra { background: var(--muted); }
+.kit-total span { color: var(--muted); }
+.kit-total strong { font-size: 1.4rem; color: var(--gold-light); }
 
-.sorteio-copy-btn {
-  background: linear-gradient(135deg, var(--gold), var(--gold-light));
-  border: none;
-  border-radius: 8px;
-  padding: 11px;
-  color: #050404;
-  font-weight: 700;
-  font-size: 0.85rem;
+.kit-add-btn { max-width: 400px; }
+.kit-add-btn:disabled { opacity: 0.4; cursor: not-allowed; }
+
+@media (max-width: 900px) {
+  .kit-3d-layout {
+    grid-template-columns: 1fr 1fr;
+    grid-template-rows: auto auto;
+  }
+  .kit-3d-center {
+    grid-column: 1 / -1;
+    order: -1;
+    position: static;
+  }
+  .kit-preview { width: 100%; flex-direction: row; }
+  .kit-preview-slot { flex: 1; }
+}
+
+@media (max-width: 600px) {
+  .kit-3d-layout { grid-template-columns: 1fr; }
+  .kit-3d-center { grid-column: auto; }
+  .kit-preview { width: 100%; flex-direction: row; gap: 8px; }
+  .kit-option img { width: 52px; height: 52px; }
+  .kit-summary { flex-direction: column; align-items: stretch; text-align: center; }
+  .kit-total { justify-content: center; }
+}
+
+/* -- CATEGORY CAROUSEL -- */
+.carousel-wrapper {
+  position: relative;
+  margin-bottom: 40px;
+}
+
+.carousel-arrow {
+  position: absolute;
+  top: 50%;
+  transform: translateY(-50%);
+  z-index: 10;
+  width: 44px;
+  height: 44px;
+  border-radius: 50%;
+  background: rgba(0,0,0,0.6);
+  border: 1px solid rgba(213,166,81,0.5);
+  color: var(--gold-light);
+  font-size: 1.2rem;
   cursor: pointer;
-  letter-spacing: 0.04em;
-  transition: opacity 0.2s;
+  display: grid;
+  place-items: center;
+  transition: background 0.2s;
 }
 
-.sorteio-copy-btn:hover { opacity: 0.88; }
+.carousel-arrow:hover { background: rgba(213,166,81,0.2); }
+.carousel-prev { left: 14px; }
+.carousel-next { right: 14px; }
 
 @media (max-width: 600px) {
-  .sorteios-grid { grid-template-columns: 1fr; }
-  .sorteio-desconto { font-size: 2rem; }
+  .carousel-arrow { width: 36px; height: 36px; font-size: 1rem; }
+  .carousel-prev { left: 8px; }
+  .carousel-next { right: 8px; }
+}
+
+.category-carousel {
+  display: flex;
+  overflow-x: auto;
+  scroll-snap-type: x mandatory;
+  margin-bottom: 40px;
+  border-radius: var(--radius);
+  cursor: grab;
+  -webkit-overflow-scrolling: touch;
+  scrollbar-width: none;
+}
+.category-carousel::-webkit-scrollbar { display: none; }
+.category-carousel.dragging { cursor: grabbing; user-select: none; }
+
+.category-banner {
+  position: relative;
+  flex: 0 0 100%;
+  scroll-snap-align: start;
+  overflow: hidden;
+  cursor: pointer;
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  aspect-ratio: 16/7;
+  transition: border-color 0.2s;
+}
+
+.category-banner:hover { border-color: rgba(213,166,81,0.5); }
+
+.category-banner img {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  filter: brightness(0.45);
+  transition: filter 0.3s;
+  pointer-events: none;
+}
+
+.category-banner:hover img { filter: brightness(0.55); }
+
+.category-banner-label {
+  position: absolute;
+  inset: 0;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-family: 'Cinzel', serif;
+  font-size: clamp(1.6rem, 4vw, 3rem);
+  font-weight: 700;
+  letter-spacing: 0.2em;
+  color: var(--text);
+  text-shadow: 0 2px 20px rgba(0,0,0,0.9);
+  pointer-events: none;
+}
+
+@media (max-width: 600px) {
+  .category-banner { aspect-ratio: 4/3; }
+  .category-banner-label { font-size: 1.4rem; }
+}
+
+/* ── CATEGORY MODAL ── */
+.category-modal-overlay {
+  position: fixed;
+  inset: 0;
+  background: rgba(0,0,0,0.85);
+  z-index: 1500;
+  display: none;
+  align-items: flex-start;
+  justify-content: center;
+  padding: 24px 16px;
+  overflow-y: auto;
+}
+
+.category-modal-overlay.open { display: flex; }
+
+.category-modal {
+  width: min(1200px, 100%);
+  background: var(--bg);
+  border: 1px solid rgba(213,166,81,0.2);
+  border-radius: 16px;
+  box-shadow: 0 24px 80px rgba(0,0,0,0.8);
+  overflow: hidden;
+  margin: auto;
+}
+
+.category-modal-header {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  padding: 20px 28px;
+  border-bottom: 1px solid var(--border);
+  background: rgba(0,0,0,0.3);
+}
+
+.category-modal-header h2 {
+  font-family: 'Cinzel', serif;
+  font-size: 1.1rem;
+  letter-spacing: 0.16em;
+  color: var(--gold-light);
+}
+
+.category-modal-header .register-close {
+  background: rgba(255,255,255,0.06);
+  border: 1px solid var(--border);
+  color: var(--text);
+}
+
+.category-modal .product-grid {
+  padding: 20px;
+  background: var(--bg);
+  grid-template-columns: repeat(5, 1fr);
+  gap: 14px;
+}
+
+.category-modal .product-card {
+  background: var(--surface);
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  box-shadow: none;
+  transition: box-shadow 0.2s, border-color 0.2s;
+}
+
+.category-modal .product-card:hover {
+  box-shadow: 0 4px 24px rgba(0,0,0,0.4);
+  border-color: rgba(213,166,81,0.4);
+  transform: none;
+  z-index: 2;
+  position: relative;
+}
+
+.category-modal .product-img-wrap {
+  background: #161313;
+  padding: 20px 16px 12px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  position: relative;
+}
+
+.category-modal .product-img-wrap::before {
+  content: '';
+  position: absolute;
+  top: 8px;
+  left: 10px;
+  width: 28px;
+  height: 28px;
+  background: url('logo.jpeg') center/cover;
+  border-radius: 50%;
+  border: 1px solid rgba(201,146,42,0.5);
+  z-index: 2;
+}
+
+.category-modal .product-img {
+  width: 100%;
+  height: 190px;
+  object-fit: contain;
+  border-bottom: none;
+  filter: none;
+}
+
+.category-modal .product-img:hover { opacity: 1; }
+
+.category-modal .product-gallery { display: none; }
+
+.category-modal .badge-units,
+.category-modal .badge-discount { display: none; }
+
+.category-modal .product-info {
+  padding: 12px 14px 10px;
+  border-top: 1px solid var(--border);
+  background: var(--surface);
+  text-align: center;
+  gap: 2px;
+}
+
+.category-modal .product-info strong {
+  font-size: 0.8rem;
+  letter-spacing: 0.05em;
+  color: var(--text);
+  font-weight: 700;
+  display: block;
+}
+
+.category-modal .product-info .product-price {
+  font-size: 0.75rem;
+  color: var(--gold-light);
+  font-weight: 600;
+}
+
+.category-modal .product-info .product-installment { display: none; }
+
+.category-modal .product-info::after {
+  content: 'HONRA \B7 LEALDADE \B7 ESTILO';
+  display: block;
+  font-size: 0.58rem;
+  letter-spacing: 0.12em;
+  color: var(--muted);
+  margin-top: 6px;
+  padding-top: 6px;
+  border-top: 1px solid var(--border);
+}
+
+.category-modal .product-card {
+  display: flex;
+  flex-direction: column;
+}
+
+.category-modal .product-info {
+  flex: 1;
+}
+
+.category-modal .add-to-cart-btn {
+  position: static;
+  width: 100%;
+  height: 38px;
+  border-radius: 0 0 var(--radius) var(--radius);
+  font-size: 0.85rem;
+  border-top: 1px solid var(--border);
+}
+
+@media (max-width: 900px) {
+  .category-modal .product-grid { grid-template-columns: repeat(3, 1fr); }
+}
+
+@media (max-width: 600px) {
+  .category-modal-overlay { padding: 0; align-items: flex-end; }
+  .category-modal {
+    border-radius: 20px 20px 0 0;
+    max-height: 88vh;
+    overflow-y: auto;
+    width: 100%;
+  }
+  .category-modal-header {
+    padding: 18px 20px 14px;
+    position: sticky;
+    top: 0;
+    z-index: 10;
+    background: var(--bg);
+  }
+  .category-modal-header h2 {
+    font-size: 1.2rem;
+    letter-spacing: 0.14em;
+    color: var(--gold-light);
+  }
+  .category-modal .product-grid {
+    grid-template-columns: repeat(2, 1fr);
+    padding: 12px;
+    gap: 12px;
+  }
+  .category-modal .product-img-wrap {
+    padding: 0;
+    background: #161313;
+  }
+  .category-modal .product-img-wrap::before { display: none; }
+  .category-modal .product-img {
+    height: 180px;
+    object-fit: cover;
+    border-radius: 0;
+    filter: none;
+  }
+  .category-modal .product-info {
+    padding: 12px 12px 8px;
+    text-align: center;
+    gap: 4px;
+  }
+  .category-modal .product-info strong {
+    font-size: 0.82rem;
+    font-weight: 800;
+    letter-spacing: 0.04em;
+    color: var(--text);
+    display: block;
+  }
+  .category-modal .product-info .product-price {
+    font-size: 0.9rem;
+    font-weight: 700;
+    color: var(--gold-light);
+  }
+  .category-modal .product-info .product-installment { display: none; }
+  .category-modal .product-info::after {
+    content: 'HONRA \B7 LEALDADE \B7 ESTILO';
+    display: block;
+    font-size: 0.6rem;
+    letter-spacing: 0.1em;
+    color: var(--muted);
+    margin-top: 6px;
+    padding-top: 6px;
+    border-top: 1px solid var(--border);
+  }
+  .category-modal .product-card {
+    display: flex;
+    flex-direction: column;
+    border-radius: 12px;
+    overflow: hidden;
+    background: #111010;
+    border: 1px solid var(--border);
+  }
+  .category-modal .add-to-cart-btn {
+    position: static;
+    width: 100%;
+    height: 42px;
+    border-radius: 0 0 12px 12px;
+    font-size: 1rem;
+    background: var(--gold);
+    border-top: 1px solid rgba(213,166,81,0.2);
+    margin-top: auto;
+  }
+  .category-modal .product-gallery { display: none; }
+  .category-modal .badge-units,
+  .category-modal .badge-discount { display: none; }
+}
+
+/* ── SORTEIOS ── */
+.sorteios-desc {
+  color: var(--muted);
+  font-size: 0.9rem;
+  margin-bottom: 28px;
+}
+
+.sorteio-loading,
+.sorteio-empty {
+  color: var(--muted);
+  font-size: 0.9rem;
+  padding: 40px;
+  text-align: center;
+  grid-column: 1 / -1;
+}
+
+.sorteios-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
+  gap: 20px;
+}
+
+.sorteio-card {
+  background: linear-gradient(135deg, #141010, #0f0d0d);
+  border: 1px solid rgba(213,166,81,0.3);
+  border-radius: 16px;
+  padding: 28px 24px;
+  display: grid;
+  gap: 12px;
+  position: relative;
+  overflow: hidden;
+  transition: transform 0.2s, border-color 0.2s;
+}
+
+.sorteio-card:hover { transform: translateY(-2px); border-color: rgba(213,166,81,0.6); }
+
+.sorteio-card::before {
+  content: '';
+  position: absolute;
+  top: -40px; right: -40px;
+  width: 120px; height: 120px;
+  border-radius: 50%;
+  background: radial-gradient(circle, rgba(213,166,81,0.12), transparent 70%);
+  pointer-events: none;
+}
+
+.sorteio-esgotado {
+  border-color: rgba(255,255,255,0.08);
+  opacity: 0.6;
+}
+
+.sorteio-esgotado:hover { transform: none; border-color: rgba(255,255,255,0.08); }
+
+.sorteio-badge {
+  display: inline-block;
+  padding: 4px 12px;
+  border-radius: 20px;
+  font-size: 0.68rem;
+  font-weight: 800;
+  letter-spacing: 0.12em;
+  width: fit-content;
+}
+
+.sorteio-card:not(.sorteio-esgotado) .sorteio-badge {
+  background: rgba(76,175,80,0.15);
+  border: 1px solid rgba(76,175,80,0.4);
+  color: #4caf50;
+}
+
+.sorteio-esgotado .sorteio-badge {
+  background: rgba(255,255,255,0.05);
+  border: 1px solid rgba(255,255,255,0.1);
+  color: var(--muted);
+}
+
+.sorteio-desconto {
+  font-family: 'Cinzel', serif;
+  font-size: 2.4rem;
+  font-weight: 900;
+  color: var(--gold-light);
+  line-height: 1;
+}
+
+.sorteio-esgotado .sorteio-desconto { color: var(--muted); }
+
+.sorteio-codigo {
+  font-size: 1.2rem;
+  font-weight: 800;
+  letter-spacing: 0.18em;
+  color: var(--text);
+  background: rgba(213,166,81,0.08);
+  border: 1px dashed rgba(213,166,81,0.35);
+  border-radius: 8px;
+  padding: 10px 14px;
+  text-align: center;
+}
+
+.codigo-riscado {
+  text-decoration: line-through;
+  color: var(--muted);
+  background: rgba(255,255,255,0.03);
+  border-color: rgba(255,255,255,0.08);
+}
+
+.sorteio-vagas {
+  font-size: 0.8rem;
+  color: var(--muted);
+}
+
+.sorteio-barra-wrap {
+  height: 5px;
+  background: rgba(255,255,255,0.06);
+  border-radius: 999px;
+  overflow: hidden;
+}
+
+.sorteio-barra {
+  height: 100%;
+  background: linear-gradient(90deg, var(--gold), var(--gold-light));
+  border-radius: 999px;
+  transition: width 0.4s ease;
+}
+
+.sorteio-esgotado .sorteio-barra { background: var(--muted); }
+
+.sorteio-copy-btn {
+  background: linear-gradient(135deg, var(--gold), var(--gold-light));
+  border: none;
+  border-radius: 8px;
+  padding: 11px;
+  color: #050404;
+  font-weight: 700;
+  font-size: 0.85rem;
+  cursor: pointer;
+  letter-spacing: 0.04em;
+  transition: opacity 0.2s;
+}
+
+.sorteio-copy-btn:hover { opacity: 0.88; }
+
+@media (max-width: 600px) {
+  .sorteios-grid { grid-template-columns: 1fr; }
+  .sorteio-desconto { font-size: 2rem; }
+}
+
+/* ═══════════════════════════════════════════════
+   MOBILE LAYOUT — max-width: 600px
+   Inspirado no layout de referência (dycashop)
+   Desktop não é afetado
+═══════════════════════════════════════════════ */
+@media (max-width: 600px) {
+
+  /* ── HEADER ── */
+  .main-header {
+    padding: 12px 16px;
+    gap: 0;
+    justify-content: space-between;
+  }
+
+  .nav-toggle { display: none; }
+
+  .nav-links { display: none !important; }
+
+  .brand-logo { height: 36px; border-radius: 50%; }
+
+  /* Nome da marca ao lado do logo */
+  .brand-name-mobile {
+    display: block;
+    font-size: 0.95rem;
+    font-weight: 800;
+    letter-spacing: 0.12em;
+    color: var(--text);
+    flex: 1;
+    margin-left: 10px;
+  }
+
+  /* Esconde busca e perfil, mantém só o carrinho */
+  .actions .icon-btn:not(.cart-icon-btn) { display: none; }
+  .actions { gap: 0; }
+
+  .cart-icon-btn {
+    width: 42px;
+    height: 42px;
+    border-radius: 10px;
+    background: rgba(255,255,255,0.07);
+    border: 1px solid var(--border);
+    font-size: 1.1rem;
+  }
+
+  /* ── HERO → card arredondado ── */
+  .hero {
+    min-height: unset;
+    padding: 12px 16px 0;
+    background: var(--bg);
+    align-items: stretch;
+    justify-content: stretch;
+  }
+
+  .hero-bg { display: none; }
+
+  .hero-content {
+    background: #161212;
+    border: 1px solid rgba(255,255,255,0.08);
+    border-radius: 18px;
+    padding: 24px 20px 24px;
+    text-align: left;
+    max-width: 100%;
+    width: 100%;
+  }
+
+  .hero-logo { display: none; }
+
+  .hero-subtitle {
+    font-size: 0.68rem;
+    letter-spacing: 0.2em;
+    color: var(--muted);
+    margin-bottom: 12px;
+    display: block;
+    text-transform: uppercase;
+  }
+
+  .hero h1 {
+    font-size: 2rem;
+    font-weight: 900;
+    line-height: 1.1;
+    text-transform: none;
+    letter-spacing: -0.02em;
+    margin-bottom: 16px;
+    color: var(--text);
+  }
+
+  .hero h1 span { color: var(--text); }
+
+  .hero-copy {
+    font-size: 0.88rem;
+    color: var(--muted);
+    margin-bottom: 0;
+    line-height: 1.6;
+  }
+
+  .hero-cta { display: none; }
+
+  /* ── FEATURES BAR ── */
+  .features-bar { display: none; }
+
+  /* ── COLEÇÕES: carousel vira grid 2×2 ── */
+  .section { padding: 20px 16px; }
+
+  .section-header {
+    margin-bottom: 16px;
+    padding-bottom: 12px;
+  }
+
+  .section-header h2 {
+    font-size: 0.95rem;
+    letter-spacing: 0.1em;
+  }
+
+  .carousel-wrapper { margin-bottom: 0; }
+
+  /* Esconde setas */
+  .carousel-arrow { display: none; }
+
+  /* Carousel vira grid 2 colunas */
+  .category-carousel {
+    display: grid !important;
+    grid-template-columns: 1fr 1fr;
+    gap: 10px;
+    overflow: visible;
+    scroll-snap-type: none;
+    cursor: default;
+    margin-bottom: 0;
+    border-radius: 0;
+  }
+
+  .category-banner {
+    flex: none;
+    width: 100%;
+    aspect-ratio: unset;
+    border-radius: 14px;
+    border: none;
+    overflow: hidden;
+    position: relative;
+    display: flex;
+    flex-direction: column;
+  }
+
+  /* Imagem ocupa parte superior */
+  .category-banner img {
+    filter: brightness(0.5);
+    width: 100%;
+    height: 130px;
+    object-fit: cover;
+    object-position: center;
+    display: block;
+    flex-shrink: 0;
+  }
+
+  /* Label fica fora da imagem, em fundo escuro sólido */
+  .category-banner-label {
+    position: static;
+    background: #111010;
+    padding: 8px 10px 10px;
+    font-family: 'Inter', sans-serif;
+    font-size: 0.78rem;
+    font-weight: 800;
+    letter-spacing: 0.03em;
+    text-align: left;
+    text-shadow: none;
+    color: var(--text);
+    line-height: 1.3;
+    display: block;
+  }
+
+  /* "+X MODELOS" via data-count attribute */
+  .category-banner-label::after {
+    content: attr(data-count);
+    display: block;
+    font-size: 0.65rem;
+    font-weight: 500;
+    letter-spacing: 0.1em;
+    color: var(--muted);
+    margin-top: 3px;
+  }
+
+  /* ── PRODUTOS grid 2 colunas compacto ── */
+  .product-grid {
+    grid-template-columns: 1fr 1fr;
+    gap: 10px;
+  }
+
+  .product-img { height: 150px; }
+
+  .product-info { padding: 8px 10px 6px; gap: 2px; }
+  .product-info strong { font-size: 0.75rem; }
+  .product-price { font-size: 0.85rem; }
+  .product-installment { font-size: 0.7rem; }
+
+  .add-to-cart-btn {
+    width: 32px; height: 32px;
+    font-size: 0.8rem;
+    bottom: 48px; right: 6px;
+  }
+
+  /* ── KIT — stack vertical ── */
+  .kit-3d-layout {
+    grid-template-columns: 1fr;
+    gap: 20px;
+  }
+
+  .kit-3d-center { order: -1; width: 100%; }
+
+  .kit-preview {
+    flex-direction: row;
+    width: 100%;
+    gap: 8px;
+  }
+
+  .kit-preview-slot { flex: 1; aspect-ratio: 3/4; }
+
+  .kit-3d-labels { width: 100%; }
+
+  /* ── TESTIMONIALS ── */
+  .testimonials { padding: 28px 16px; }
+  .testimonials-inner { grid-template-columns: 1fr; gap: 20px; }
+  .review-cards { grid-template-columns: 1fr; gap: 10px; }
+
+  /* ── NEWSLETTER ── */
+  .newsletter { padding: 24px 16px; flex-direction: column; gap: 16px; }
+  .newsletter-form { width: 100%; max-width: 100%; flex-direction: column; }
+  .newsletter-form input, .newsletter-form button { width: 100%; }
+
+  /* ── FOOTER ── */
+  .footer { grid-template-columns: 1fr; padding: 28px 16px 16px; gap: 20px; }
+  .footer-brand { grid-column: auto; }
+  .footer-logo { height: 52px; }
+
+  /* Esconde colunas de links no footer mobile, mantém só brand */
+  .footer-col { display: none; }
+
+  /* ── SORTEIOS ── */
+  .sorteios-grid { grid-template-columns: 1fr; }
+}
+
+/* ── KIT PICKER ── */
+.kit-picker-layout {
+  display: grid;
+  grid-template-columns: 320px 1fr;
+  gap: 32px;
+  align-items: start;
+  margin-bottom: 36px;
+}
+
+.kit-picker-btns {
+  display: flex;
+  flex-direction: column;
+  gap: 14px;
+  justify-content: center;
+}
+
+.kit-picker-btn {
+  display: flex;
+  align-items: center;
+  gap: 16px;
+  background: var(--surface);
+  border: 2px solid var(--border);
+  border-radius: var(--radius);
+  padding: 18px 20px;
+  cursor: pointer;
+  text-align: left;
+  transition: border-color 0.2s, background 0.2s;
+  width: 100%;
+  color: var(--text);
+}
+
+.kit-picker-btn:hover {
+  border-color: rgba(213,166,81,0.5);
+  background: rgba(213,166,81,0.05);
+}
+
+.kit-picker-btn-done {
+  border-color: var(--gold-light);
+  background: rgba(213,166,81,0.07);
+}
+
+.kit-picker-icon { font-size: 1.6rem; flex-shrink: 0; }
+
+.kit-picker-label {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+}
+
+.kit-picker-label strong {
+  font-size: 0.82rem;
+  letter-spacing: 0.1em;
+  color: var(--text);
+}
+
+.kit-picker-label small {
+  font-size: 0.78rem;
+  color: var(--muted);
+}
+
+.kit-picker-btn-done .kit-picker-label small { color: var(--gold-light); }
+
+.kit-picker-arrow {
+  font-size: 1.4rem;
+  color: var(--muted);
+  flex-shrink: 0;
+}
+
+/* Grid dentro do modal picker — igual ao category modal */
+.kit-picker-grid {
+  padding: 20px;
+  grid-template-columns: repeat(4, 1fr);
+  gap: 14px;
+  cursor: pointer;
+}
+
+.kit-picker-grid .product-card {
+  transition: border-color 0.2s, transform 0.15s;
+}
+
+.kit-picker-grid .product-card:hover {
+  border-color: var(--gold-light);
+  transform: translateY(-2px);
+}
+
+@media (max-width: 900px) {
+  .kit-picker-layout { grid-template-columns: 1fr; }
+  .kit-picker-layout .kit-3d-center { order: -1; }
+  .kit-preview { flex-direction: row; width: 100%; }
+  .kit-preview-slot { flex: 1; }
+  .kit-picker-grid { grid-template-columns: repeat(3, 1fr); }
+}
+
+@media (max-width: 600px) {
+  .kit-picker-layout { gap: 20px; }
+  .kit-picker-btn { padding: 14px 16px; gap: 12px; }
+  .kit-picker-icon { font-size: 1.3rem; }
+  .kit-picker-grid {
+    grid-template-columns: repeat(2, 1fr);
+    padding: 12px;
+    gap: 12px;
+  }
+  /* Kit picker modal = mesmo estilo do category modal mobile */
+  #kitPickerOverlay { padding: 0; align-items: flex-end; }
+  #kitPickerOverlay .category-modal {
+    border-radius: 20px 20px 0 0;
+    max-height: 88vh;
+    overflow-y: auto;
+    width: 100%;
+  }
+  #kitPickerOverlay .category-modal-header {
+    padding: 18px 20px 14px;
+    position: sticky;
+    top: 0;
+    z-index: 10;
+    background: var(--bg);
+  }
+  #kitPickerOverlay .category-modal-header h2 {
+    font-size: 1.2rem;
+    color: var(--gold-light);
+  }
+  .kit-picker-grid .product-card {
+    display: flex;
+    flex-direction: column;
+    border-radius: 12px;
+    overflow: hidden;
+    background: #111010;
+    border: 1px solid var(--border);
+  }
+  .kit-picker-grid .product-img-wrap {
+    padding: 0;
+    background: #161313;
+  }
+  .kit-picker-grid .product-img-wrap::before { display: none; }
+  .kit-picker-grid .product-img {
+    height: 180px;
+    object-fit: cover;
+    border-radius: 0;
+    filter: none;
+    border-bottom: none;
+  }
+  .kit-picker-grid .product-info {
+    padding: 12px 12px 8px;
+    text-align: center;
+    gap: 4px;
+    flex: 1;
+  }
+  .kit-picker-grid .product-info strong {
+    font-size: 0.82rem;
+    font-weight: 800;
+    color: var(--text);
+    display: block;
+  }
+  .kit-picker-grid .product-info .product-price {
+    font-size: 0.9rem;
+    font-weight: 700;
+    color: var(--gold-light);
+  }
+  .kit-picker-grid .product-info .product-installment { display: none; }
+  .kit-picker-grid .product-info::after {
+    content: 'HONRA \B7 LEALDADE \B7 ESTILO';
+    display: block;
+    font-size: 0.6rem;
+    letter-spacing: 0.1em;
+    color: var(--muted);
+    margin-top: 6px;
+    padding-top: 6px;
+    border-top: 1px solid var(--border);
+  }
+  .kit-picker-grid .product-gallery { display: none; }
+  .kit-picker-grid .badge-units,
+  .kit-picker-grid .badge-discount { display: none; }
+  .kit-picker-grid .add-to-cart-btn { display: none; }
+}
+
+/* ── BANNER TOPO DO MODAL DE CATEGORIA ── */
+.cat-modal-banner {
+  display: none; /* oculto no desktop */
+}
+
+@media (max-width: 600px) {
+  .cat-modal-banner {
+    display: flex;
+    align-items: stretch;
+    position: relative;
+    overflow: hidden;
+    background: #080808;
+    border-bottom: 1px solid rgba(213,166,81,0.15);
+    height: 160px;
+    padding: 0;
+  }
+
+  .cat-modal-banner::before {
+    content: '';
+    position: absolute;
+    inset: 0;
+    background:
+      linear-gradient(120deg, rgba(213,166,81,0.22) 0%, transparent 40%),
+      linear-gradient(300deg, rgba(213,166,81,0.12) 0%, transparent 40%);
+    pointer-events: none;
+    z-index: 1;
+  }
+
+  .cat-modal-banner::after {
+    content: '';
+    position: absolute;
+    inset: 0;
+    background: repeating-linear-gradient(
+      -52deg,
+      transparent,
+      transparent 20px,
+      rgba(213,166,81,0.035) 20px,
+      rgba(213,166,81,0.035) 21px
+    );
+    pointer-events: none;
+    z-index: 1;
+  }
+
+  .cat-modal-banner-text {
+    position: relative;
+    z-index: 2;
+    display: flex;
+    flex-direction: column;
+    justify-content: center;
+    gap: 5px;
+    width: 52%;
+    padding: 16px 0 16px 16px;
+  }
+
+  .cat-modal-banner-sub {
+    font-size: 0.58rem;
+    letter-spacing: 0.24em;
+    color: var(--gold-light);
+    font-weight: 700;
+    text-transform: uppercase;
+  }
+
+  .cat-modal-banner-title {
+    font-size: 1.65rem;
+    font-weight: 900;
+    line-height: 1.0;
+    letter-spacing: -0.02em;
+    color: #fff;
+    text-transform: uppercase;
+    display: block;
+  }
+
+  .cat-modal-banner-tags {
+    font-size: 0.55rem;
+    letter-spacing: 0.1em;
+    color: rgba(255,255,255,0.45);
+    margin-top: 4px;
+  }
+
+  .cat-modal-banner-img {
+    position: absolute;
+    z-index: 2;
+    right: 0;
+    top: 0;
+    width: 52%;
+    height: 100%;
+    object-fit: cover;
+    object-position: center center;
+    border-radius: 0;
+    filter: brightness(0.88);
+    -webkit-mask-image: linear-gradient(to right, transparent 0%, rgba(0,0,0,0.6) 20%, black 50%);
+    mask-image: linear-gradient(to right, transparent 0%, rgba(0,0,0,0.6) 20%, black 50%);
+  }
+}
+
+/* ── FILTRO DE TAMANHO ── */
+.size-filter {
+  padding: 14px 20px 0;
+}
+
+.size-filter-inner {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 8px;
+  align-items: center;
+  padding-bottom: 14px;
+  border-bottom: 1px solid var(--border);
+}
+
+.size-btn {
+  background: rgba(255,255,255,0.05);
+  border: 1px solid var(--border);
+  color: var(--muted);
+  border-radius: 8px;
+  padding: 7px 18px;
+  font-size: 0.82rem;
+  font-weight: 700;
+  letter-spacing: 0.06em;
+  cursor: pointer;
+  transition: background 0.15s, border-color 0.15s, color 0.15s;
+}
+
+.size-btn:hover {
+  border-color: rgba(213,166,81,0.5);
+  color: var(--text);
+}
+
+.size-btn-active {
+  background: linear-gradient(135deg, var(--gold), var(--gold-light));
+  border-color: transparent;
+  color: #050404;
+}
+
+@media (max-width: 600px) {
+  .size-filter { padding: 12px 16px 0; }
+  .size-filter-inner { gap: 8px; padding-bottom: 12px; }
+  .size-btn { padding: 8px 20px; font-size: 0.85rem; border-radius: 10px; }
+}
+
+/* ── MOBILE CATEGORY MODAL — PREMIUM LAYOUT ── */
+@media (max-width: 600px) {
+  .category-modal-overlay { padding: 0; align-items: flex-end; }
+
+  .category-modal {
+    border-radius: 20px 20px 0 0;
+    max-height: 88vh;
+    overflow-y: auto;
+    width: 100%;
+  }
+
+  .category-modal-header {
+    padding: 18px 20px 14px;
+    position: sticky;
+    top: 0;
+    z-index: 10;
+    background: var(--bg);
+  }
+
+  .category-modal-header h2 {
+    font-size: 1.2rem;
+    letter-spacing: 0.14em;
+    color: var(--gold-light);
+  }
+
+  /* 1 coluna */
+  .category-modal .product-grid {
+    grid-template-columns: 1fr !important;
+    padding: 16px !important;
+    gap: 16px !important;
+  }
+
+  /* Card premium */
+  .category-modal .product-card {
+    display: flex !important;
+    flex-direction: column !important;
+    border-radius: 16px !important;
+    overflow: hidden !important;
+    background: #111010 !important;
+    border: 1px solid rgba(255,255,255,0.08) !important;
+    box-shadow: 0 4px 24px rgba(0,0,0,0.5) !important;
+    transform: none !important;
+  }
+
+  /* Foto 65% do card */
+  .category-modal .product-img-wrap {
+    padding: 0 !important;
+    background: #161313 !important;
+    position: relative !important;
+  }
+
+  .category-modal .product-img-wrap::before { display: none !important; }
+
+  .category-modal .product-img {
+    width: 100% !important;
+    height: 260px !important;
+    object-fit: cover !important;
+    border-radius: 0 !important;
+    filter: none !important;
+    border-bottom: none !important;
+    display: block !important;
+  }
+
+  /* Badges visíveis */
+  .category-modal .badge-units {
+    display: block !important;
+    position: absolute !important;
+    top: 12px !important;
+    left: 12px !important;
+    background: rgba(220,50,50,0.92) !important;
+    color: #fff !important;
+    font-size: 0.72rem !important;
+    font-weight: 800 !important;
+    padding: 5px 12px !important;
+    border-radius: 8px !important;
+    z-index: 2 !important;
+  }
+
+  .category-modal .badge-discount {
+    display: block !important;
+    position: absolute !important;
+    top: 12px !important;
+    left: 12px !important;
+    font-size: 0.72rem !important;
+    font-weight: 800 !important;
+    padding: 5px 12px !important;
+    border-radius: 8px !important;
+    z-index: 2 !important;
+  }
+
+  .category-modal .product-gallery { display: none !important; }
+
+  /* Info */
+  .category-modal .product-info {
+    padding: 16px 16px 10px !important;
+    text-align: left !important;
+    gap: 4px !important;
+    border-top: 1px solid rgba(255,255,255,0.06) !important;
+  }
+
+  .category-modal .product-info strong {
+    font-size: 1.05rem !important;
+    font-weight: 700 !important;
+    color: var(--text) !important;
+    display: block !important;
+    text-transform: capitalize !important;
+    line-height: 1.3 !important;
+    letter-spacing: 0 !important;
+  }
+
+  .category-modal .product-info .product-price {
+    font-size: 1.5rem !important;
+    font-weight: 800 !important;
+    color: var(--gold-light) !important;
+    display: block !important;
+    margin-top: 8px !important;
+  }
+
+  .category-modal .product-info .product-installment {
+    display: block !important;
+    font-size: 0.8rem !important;
+    color: var(--muted) !important;
+    margin-top: 2px !important;
+  }
+
+  /* Remove tagline */
+  .category-modal .product-info::after { display: none !important; }
+
+  /* Botão COMPRAR dourado full-width */
+  .category-modal .add-to-cart-btn {
+    position: static !important;
+    width: calc(100% - 32px) !important;
+    height: 48px !important;
+    margin: 6px 16px 16px !important;
+    border-radius: 12px !important;
+    background: linear-gradient(135deg, var(--gold), var(--gold-light)) !important;
+    color: #050404 !important;
+    font-size: 0.92rem !important;
+    font-weight: 800 !important;
+    letter-spacing: 0.08em !important;
+    border: none !important;
+    display: flex !important;
+    align-items: center !important;
+    justify-content: center !important;
+    gap: 8px !important;
+  }
+
+  .category-modal .add-to-cart-btn::after {
+    content: 'COMPRAR';
+  }
 }
